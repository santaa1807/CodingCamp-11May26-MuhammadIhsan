{
  "name": "expense-budget-visualizer",
  "version": "1.0.0",
  "description": "Expense & Budget Visualizer — zero-dependency client-side app",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0",
    "fast-check": "^3.22.0"
  }
}
